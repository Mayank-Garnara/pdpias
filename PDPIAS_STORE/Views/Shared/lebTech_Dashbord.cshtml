<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>PDPias - Principal Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
          rel="stylesheet" />
    <link rel="stylesheet"
          href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" />
    <link rel="stylesheet"
          href="https://cdn.jsdelivr.net/npm/chart.js@3.7.1/dist/chart.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    @*<link rel="stylesheet" href="style.css" />*@
    <link rel="stylesheet" href="~/Style/StyleSheet1.css" />
</head>
<body>
    <!-- Header Section -->
    <header class="custom-header" style="background-color: #6c63ff ">
        <div class="d-flex align-items-center">
            <!-- Mobile Menu Toggle Button -->
            <button class="mobile-menu-toggle"
                    id="menuToggle"
                    aria-label="Toggle menu">
                <i class="bi bi-list"></i>
            </button>

            <!-- Logo/Brand Name with Icon -->
            <a href="#" class="brand-name">
                <i class="bi bi-building-gear"></i>
                <span>PDPias Store</span>
            </a>
        </div>
        <!-- Right Side Items -->
        <div class="d-flex align-items-center">
            <!-- Notification Bell -->
            <a href="@Url.Action("NotificationsAlerts","labTech")" class="notification-icon" aria-label="Notifications">
                <i class="bi bi-bell"></i>
                <span class="notification-badge">3</span>
            </a>

            <!-- Profile Dropdown -->
            <a href="#"
               class="text-decoration-none dropdown-toggle d-flex justify-content-center align-items-center"
               data-bs-toggle="dropdown"
               aria-expanded="false"
               id="userDropdown"
               aria-haspopup="true">
                <div class="profile-container d-flex">
                    <img src="https://randomuser.me/api/portraits/men/50.jpg"
                         alt="Profile"
                         class="profile-img"
                         loading="lazy" />

                </div>
                @*<span class="profile-name ms-1">Dr. Kavit Bokarvadiya</span>*@
            </a>
            <div class="dropdown">

                <ul class="dropdown-menu dropdown-menu-end"
                    aria-labelledby="userDropdown">
                    <li>
                        <a class="dropdown-item" href="#profile-section">
                            <i class="bi bi-person"></i> Profile
                        </a>
                    </li>
                    <li>
                        <a class="dropdown-item" href="#">
                            <i class="bi bi-gear"></i> Settings
                        </a>
                    </li>
                    <li><hr class="dropdown-divider" /></li>
                    <li>
                        <!-- Change both logout buttons to use this class -->
                        <a href="#" class="nav-link logout-btn">
                            <i class="bi bi-box-arrow-right"></i>
                            <span>Logout</span>
                        </a>
                    </li>
                </ul>

            </div>
        </div>
    </header>

    <!-- Sidebar Navigation -->
    <nav class="sidebar" id="sidebar">
        <ul class="nav-menu">
            <li class="nav-item">
                <a href="@Url.Action("Index","labTech")" class="nav-link">
                    <i class="fas fa-tachometer-alt"></i>
                    <span>Dashboard</span>
                </a>
            </li>
            <li class="nav-item">
                <a href="@Url.Action("ChemicalOrder","labTech")" class="nav-link">
                    <i class="fas fa-prescription-bottle-medical"></i>
                    <span>Chemical Order</span>
                </a>
            </li>
            <li class="nav-item">
                <a href="@Url.Action("SearchChemical","labTech")" class="nav-link">
                    <i class="fas fa-prescription-bottle-medical"></i>
                    <span>Search Chemical</span>
                </a>
            </li>
            <li class="nav-item">
                <a href="@Url.Action("GlasswareOrder","labTech")" class="nav-link">
                    <i class="fas fa-prescription-bottle-medical"></i>
                    <span>Glassware Order</span>
                </a>
            </li>
            @*<li class="nav-item">
                <a href="@Url.Action("NotificationsAlerts","labTech")" class="nav-link">
                    <i class="fas fa-bell"></i>
                    <span>Notifications / Alerts</span>
                </a>
            </li>*@
            <li class="nav-item">
                <a href="@Url.Action("ProfileSettings","labTech")" class="nav-link">
                    <i class="fas fa-user-cog"></i>
                    <span>Profile Settings</span>
                </a>
            </li>
            <li class="nav-item">
                <a href="@Url.Action("LogOut","labTech")" class="nav-link">
                    <i class="fas fa-right-from-bracket"></i>
                    <span>Log Out</span>
                </a>
            </li>
            @*<li class="nav-item storage-parent">
                    <a href="#!" class="nav-link storage-toggle">
                        <i class="bi bi-box-seam"></i>
                        <span>User Management</span>
                        <i class="bi bi-chevron-down ms-auto small"></i>
                    </a>
                    <ul class="nav flex-column storage-submenu">
                        <li class="nav-item">
                            <a href="@Url.Action("UserManagement", "Principal", new { category = "AllUser" })" class="nav-link sub-link">
                                - All User
                            </a>
                        </li>
                        <li class="nav-item">
                            <a href="#current-glassware-section" class="nav-link sub-link">
                                - Pending User
                            </a>
                        </li>
                        <li class="nav-item">
                            <a href="#add-stock-section" class="nav-link sub-link">
                                - Rejected User
                            </a>
                        </li>
                    </ul>
                </li>*@

            @*<li class="nav-item">
                    <a href="@Url.Action("Reports","Principal")" class="nav-link">
                        <i class="bi bi-clipboard-data"></i>
                        <span>Reports</span>
                    </a>
                </li>*@
            @*<li class="nav-item">
                    <a href="@Url.Action("Profile","Principal")" class="nav-link">
                        <i class="bi bi-person"></i>
                        <span>Profile</span>
                    </a>
                </li>*@
            @*<li class="nav-item">
                    <a href="#" class="nav-link" id="sidebarLogoutBtn">
                        <i class="bi bi-box-arrow-right"></i>
                        <span>Logout</span>
                    </a>
                </li>*@
        </ul>
    </nav>

    <div>
        @RenderBody()
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js@3.7.1/dist/chart.min.js"></script>
    @*<script src="~/Scripts/script.js"></script>*@



    <script>
        //for the active class dynamic
        // Get current path (without query string or hash)
        const currentPath = window.location.pathname.toLowerCase();

        // Loop through all sidebar nav links
        document.querySelectorAll('.nav-link').forEach(link => {
            const linkPath = link.getAttribute('href').toLowerCase();

            // If the link path matches the current path, add 'active' class
            if (linkPath === currentPath) {
                link.classList.add('active');
            }
        });
    </script>
    <script>
        // Storage Expand/Collapse Animation
        const storageParent = document.querySelector(".storage-parent");
        const storageToggleBtn = document.querySelector(".storage-toggle");
        const storageSubmenu = document.querySelector(".storage-submenu");

        storageToggleBtn.addEventListener("click", function (e) {
            e.preventDefault();

            if (storageSubmenu.style.height === "0px" || storageSubmenu.style.height === "0") {
                const fullHeight = storageSubmenu.scrollHeight + "px";
                storageSubmenu.style.height = fullHeight;
                storageParent.classList.add("active");
                storageToggleBtn.querySelector(".bi-chevron-down").style.transform = "rotate(180deg)";
            } else {
                storageSubmenu.style.height = "0";
                storageParent.classList.remove("active");
                storageToggleBtn.querySelector(".bi-chevron-down").style.transform = "rotate(0deg)";
            }
        });

        // Nav active class on sub-link click
        document.querySelectorAll(".storage-submenu .nav-link.sub-link").forEach(link => {
            link.addEventListener("click", function () {
                document.querySelectorAll(".storage-submenu .nav-link.sub-link").forEach(l => l.classList.remove("active"));
                this.classList.add("active");
            });
        });

        document.querySelectorAll(".storage-submenu .nav-link.sub-link").forEach(link => {
            link.addEventListener("click", function () {
                document.querySelectorAll(".storage-submenu .nav-link.sub-link").forEach(l => l.classList.remove("active"));
                this.classList.add("active");
            });
        });F
    </script>

</body>
</html>
